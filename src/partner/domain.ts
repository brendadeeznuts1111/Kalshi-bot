/**
 * @deprecated Import from `./architecture.ts` — this path kept for one release.
 * Seat-ops architecture (not desk matrix `src/domain/`).
 */
export {
  OPS_LAYERS,
  PARTNER_DOMAIN_LAYERS,
  PARTNER_NAMING,
  buildDomainStatusReport,
  formatDomainStatusText,
  formatPartnerExpansionMermaid,
  type DomainComponent,
  type DomainLayer,
  type DomainLayerId,
  type DomainMaturity,
  type DomainStatusReport,
} from './architecture.ts';
